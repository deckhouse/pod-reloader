---
image: release-channel-version
from: {{ index $.Images "base/scratch" }}
import:
  - image: images-digests-and-version
    add: /
    to: /
    after: install
    includePaths:
      - version.json
      - changelog.yaml
      - module.yaml