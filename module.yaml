name: pod-reloader
weight: 900
stage: "General Availability"
subsystems:
  - infrastructure
namespace: d8-pod-reloader
requirements:
  bootstrapped: true
  deckhouse: ">= 1.68"

accessibility:
  editions:
    _default:
      available: true
      enabledInBundles:
        - Default
        - Managed