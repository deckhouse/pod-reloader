name: pod-reloader
descriptions:
  en: "Provides the ability for automatic rollout in case of changes to ConfigMap or Secret resources."
  ru: "Предоставляет возможность автоматически произвести rollout в случае изменения ресурсов ConfigMap или Secret."
weight: 900
stage: "General Availability"
subsystems:
  - infrastructure
namespace: d8-pod-reloader
requirements:
  bootstrapped: true
  deckhouse: ">= 1.68"

accessibility:
  editions:
    _default:
      available: true
      enabledInBundles:
        - Default
        - Managed